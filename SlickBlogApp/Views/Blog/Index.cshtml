@model SlickBlogApp.ViewModels.CpPosts
@using SlickBlogApp.Helpers
@{
    Layout = "~/Views/Shared/_LayoutDisplay.cshtml";
    ViewBag.Title = Model.Title;
}
@*@section scripts {
<script src="@Url.Content("~/Scripts/jquery.unobtrusive-ajax.min.js")" type="text/javascript"></script>
<script type="text/javascript">  
    function done() {
        $('textarea').empty();
    }
    $('.ajaxMore').live("click", function () {
        var skip = $(this).attr('data-skip');
        var postId = this.id;
        var updateDiv = '#MoreCommentsDiv' + postId;
        $.get(this.href+'/'+skip, function (data) {
            $(updateDiv).append(data);
        }, 'html');
        skip++;
        $(this).attr('data-skip', skip);
        return false;
    });

    $('.ajaxComments').live("click", function () {
        
        $(this).addClass('hideComments').removeClass('ajaxComments');
        
        var postId = this.id;
        var updateDiv = '#AjaxCommentDiv' + postId;
        $(updateDiv).load(this.href);
        return false;
    });


    $('.hideComments').live('click', function () {
            event.preventDefault();
            var postId = this.id;
            var updateDiv = '#AjaxCommentDiv' + postId;
            $(this).addClass('ajaxComments').removeClass('hideComments');
            $(updateDiv).empty();
        });


</script>
}*@
<h1>@Model.Title</h1>
<section id="gridSystem">
<div class="row">
    @if (Model.Posts.Count() > 0)
    {
        foreach (var post in Model.Posts)
        {
            @Html.Partial("_Post", post)
            @*<div class="span12 sba-post">
                <p><span class="label label-info">Posted by @post.Author.Username, on @post.PostDate</span></p>
                <h2>@post.Title</h2>
                @MvcHtmlString.Create(post.Content)
                <br /><br />
                <p><a class="btn ajaxComments" id="@post.PostId"href="@Url.Content("~/Blog/" + Model.Address + "/PostComments/"+post.PostId)">Comments »</a></p>
                <div id="@MvcHtmlString.Create("AjaxCommentDiv"+post.PostId)"></div>
            </div>*@
        
            
        
        }
    }
    else
    {
        <div class="span12 sba-post">
            <p><span class="label label-info">Auto message</span></p>
            <h2>Welcome to my blog!</h2>
            <p>No posts yet.</p>
        </div>
    }
</div>
@Html.PostPaging(Model)
</section>
